#!/usr/bin/as3shebang -- 

import shell.*;

/* ATTENTION
   from a shell script you can not run workers
   SimpleSocketServerSelect5 and following will fail
   you have to run from the redshell directly
   for ex:
   $ ./redshell_dd spitfire.abc
*/

//var clilib:* = Domain.currentDomain.load( "lib-abc/clilib.abc" );
//trace( clilib + " loaded" );

//var ansilib:* = Domain.currentDomain.load( "lib-abc/ansilib.abc" );
//trace( ansilib + " loaded" );

//var hashlib:* = Domain.currentDomain.load( "lib-abc/hashlib.abc" );
//trace( hashlib + " loaded" );

var spitfire:* = Domain.currentDomain.load( "spitfire.abc" );
trace( spitfire + " loaded" );

trace( "hello world" );

